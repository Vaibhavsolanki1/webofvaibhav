export default function IntroSection() {
  return (
    <section id="intro" className="section bg-slate-950">
      <div className="mx-auto flex max-w-4xl flex-col gap-4 px-6">
        <p className="eyebrow">Intro</p>
        <h2 className="headline">It started with curiosity.</h2>
        <p className="subhead">
          A need to understand how emotion and logic could live in the same
          interface.
        </p>
      </div>
    </section>
  );
}
